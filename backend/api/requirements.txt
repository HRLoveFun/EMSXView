# EMSX Trading API - Python Dependencies
# Install: pip install -r requirements.txt
#
# Internal monorepo packages — REQUIRED (installed as editable):
#   pip install -e ../../DataPipeline --no-deps
#   pip install -e ../../platform_data --no-deps
#   pip install -e ../../CostView --no-deps
#
# The --no-deps flag avoids circular dependency resolution during dev setup.
# In production (Docker), all three are installed sequentially per Dockerfile.

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Bloomberg API
blpapi==3.23.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Async Support
asyncio-mqtt==0.16.1
websockets==12.0

# Logging & Monitoring
structlog==24.1.0
prometheus-client==0.19.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1
orjson==3.9.10

# Persistence (Sprint 1)
sqlalchemy==2.0.37
asyncpg==0.30.0
alembic==1.14.1
psycopg[binary]==3.2.3

# Development (optional)
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==6.0.0
httpx==0.26.0

