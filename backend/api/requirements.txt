# EMSX Trading API - Python Dependencies
# Install: pip install -r requirements.txt
#
# Internal monorepo packages — REQUIRED (installed as editable):
#   pip install -e ../../DataPipeline --no-deps
#   pip install -e ../../platform_data --no-deps
#   pip install -e ../../CostView --no-deps
#
# The --no-deps flag avoids circular dependency resolution during dev setup.
# In production (Docker), all three are installed sequentially per Dockerfile.

# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Bloomberg API
# 注意：blpapi 不在公共 PyPI，直接安装会报 "No matching distribution found"，
# 必须附加 Bloomberg 私有源：
#   pip install -r requirements.txt --extra-index-url=https://blpapi.bloomberg.com/repository/releases/python/simple/
# 版本需与 Bloomberg Terminal 的 C++ 运行时匹配；wheel 自带 blpapi3_64.dll，
# 升级 pip 包即同步升级 C++ 运行时，无需单独安装 C++ SDK。
blpapi==3.26.7.1

# Authentication & Security
# P2-6 整改：python-jose 已停止维护且存在已知 CVE（CVE-2024-33663/33664），迁移到 PyJWT
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Async Support
asyncio-mqtt==0.16.1
websockets==12.0

# Logging & Monitoring
structlog==24.1.0
prometheus-client==0.19.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1
orjson==3.9.10

# Persistence (Sprint 1)
sqlalchemy==2.0.37
asyncpg==0.30.0
alembic==1.14.1
psycopg[binary]==3.2.3

# Development (optional)
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==6.0.0
httpx==0.26.0

