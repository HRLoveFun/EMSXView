[pytest]
testpaths = tests/guardrail
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
timeout = 300
markers =
    slow: Mark slow tests
    integration: Mark integration tests
    unit: Mark unit tests
