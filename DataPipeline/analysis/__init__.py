"""
DataPipeline analysis sub-package.

Contains regime classification and attribution metrics computation,
previously living in CostView/src/regime/ and CostView/src/attribution/.

These modules are core to the Data Pipeline and operate on regime.db
and processed_fills.db — they do NOT depend on CostView.
"""
