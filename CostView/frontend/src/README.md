# Archived Legacy Source

The legacy prototype source that previously lived under this directory was archived to:

- `CostView/frontend/archive/2026-04-22/src/`

This directory is intentionally left as a marker so future work does not restart from the deprecated prototype surface.

Use these canonical locations instead:

- Active frontend shell: `Execution/frontend/src/App.tsx`
- Active CostView module: `Execution/frontend/src/modules/costview/`
- Active CostView analytics/data layer: `CostView/src/`
