## 执行成本分析系统落地方案

---

## 1. 方案背景与设计目标

本方案用于买方多算法执行、以 VWAP 为主的生产环境，建立科学、统一、数据可行的执行成本分析体系。可获取数据为：

- **订单与成交**：母单/子单基本信息、成交流水（成交价、成交量、时间戳）
- **市场数据**：证券及基准指数 10 秒级 OHLC（开、高、低、收）、成交量、成交额
- **元数据**：证券 ADV、日波动率、订单规模与方向、限价/市价标记、日度买卖价差均值

方案核心能力：

1. **统一经济损益语言**：以到达价合成中间价为基准的执行差额（IS）作为跨算法、跨路由的唯一比较货币
2. **恒等拆分**：将 IS 无残差地拆分为算法执行贡献、等待漂移与工作区间调度漂移
3. **微观诊断**：进一步拆解为流动性成本、择时分布成本和市场环境成本，服务 VWAP 运营
4. **统计推断**：通过分布描述、bootstrap 置信区间和分层比较降低噪声、提升结论可靠性

与初版方案的关键变化：
- **基准价格**：由区间 (High+Low)/2 改为区间 **Open（开盘价）**，消除前视偏差（Look-Ahead Bias）与范围中点偏差
- **移除预期成本模型**：当前基建不具备模型持续部署与校准能力，不再计算 E[IS] 及 Score，路由比较直接基于 IS 及算法净贡献
- **移除紧急度分组与算法风格聚类**：避免引入不可靠的事后分类变量
- **移除刷基准检测**：正交条件设计导致假阳性率过高，不具备可操作性

---

## 2. 数据采集与预处理

### 2.1 订单与成交表

**母单表（Parent Order）**

| 字段 | 来源 | 说明 |
|------|------|------|
| parent_id | OMS | 母单唯一标识 |
| symbol | OMS | 证券代码 |
| side | OMS | +1 买入，-1 卖出 |
| qty_parent | OMS | 委托数量 |
| arrival_time_parent | OMS | 母单下达系统时间（均为连续交易时段） |
| arrival_price_parent (合成) | 10s OHLC | 到达时刻所在区间的 **Open** |
| order_type | OMS | 限价/市价 |
| limit_price | OMS | 限价 |
| qty_executed_total | 聚合 | 所有子单成交总和 |
| status | OMS | 完成/部分成交/取消 |
| is_close_participation | OMS/规则 | 是否为收盘参与型订单（如目标收盘价） |

**子单/路由表（Route）**

| 字段 | 来源 | 说明 |
|------|------|------|
| route_id | OMS | 唯一标识 |
| parent_id | OMS | 关联母单 |
| broker | OMS | 经纪商 |
| algo | OMS | 算法类型（VWAP/TWAP/IS 等） |
| time_sent | OMS | 发送至路由时刻 |
| arrival_price_route (合成) | 10s OHLC | 对应发送时刻区间的 **Open** |
| qty_route | OMS | 委托数量 |
| qty_filled | OMS | 实际成交数量 |
| time_first_fill | OMS | 首笔成交时刻 |
| time_last_fill | OMS | 末笔成交时刻 |
| status | OMS | 完成/部分成交/取消 |

**成交流水表（Execution）**

| 字段 | 来源 | 说明 |
|------|------|------|
| exec_id | OMS/经纪商 | 成交编号 |
| route_id | OMS | 所属路由 |
| timestamp | OMS | 成交时刻 |
| price | OMS | 成交价 |
| qty | OMS | 成交量 |

### 2.2 市场数据

基于证券及基准指数（如沪深 300 或对应 A 股指数）的 10 秒级 OHLC 构建以下衍生序列：

- **合成中间价（Mid Proxy）**：  
  `mid_proxy = Open`  
  使用区间开盘价作为该区间代理基准价。理由：(1) Open 为区间起点已知价格，无前视偏差；(2) Open 是实际成交价或首笔报价，比 (High+Low)/2 范围中点更具经济含义；(3) 消除范围中点偏差在恒等式中的系统性传播。若 Open 缺失，使用前一区间 Close 回填，并记录缺失比例作为数据质量监控项。

- **区间 VWAP**（用于市场均价）：  
  `VWAP_interval = Σ (成交额_i) / Σ 成交量_i`，其中 i 遍历区间内全部 10 秒切片。

- **区间中间价加权均价**：  
  `VWAP_interval,mid = Σ (mid_proxy_i × 成交量_i) / Σ 成交量_i`  
  其中 `成交量_i` 为区间第 i 个 10 秒切片的**市场成交量（股数）**，与计算 VWAP_interval 所用的成交量来源一致。此序列用于计算择时分布成本。

- **反转后价格**：  
  以路由末笔成交后 T+5 分钟、T+30 分钟所在 10 秒区间的 **Open** 作为反转后价格 `P_post`，与到达基准保持一致，避免买卖价差反弹干扰。若区间为非交易时段，取最近一个区间。

- **日内波动率**：  
  取到达时刻前 30 分钟（含当前）的全部 10 秒 Open，计算对数收益率序列。设此序列标准差为 `σ_10s`，交易日总 10 秒区间数为 `N_day`，由数据库 `exchange` 列映射对应交易时长：

  | Exchange | 交易时长 | N_day |
  |----------|----------|-------|
  | A 股（上证/深证） | 4 小时（9:30-11:30, 13:00-15:00） | 1440 |
  | 港股（港交所） | 5.5 小时（9:30-12:00, 13:00-16:00） | 1980 |
  | 美股（NYSE/NASDAQ） | 6.5 小时 | 2340 |

  日化波动率 `σ_daily = σ_10s × √N_day`，单位为比例值；转化为 bps 时乘以 10^4。年化波动率 `σ_annual = σ_daily × √252`。

- **价差代理**：  
  优先使用系统可获取的"日度买卖价差均值"（前一日全部 Ask – Bid 均值），除以当日 Open 日均值，记为 `spread_proxy`。原始数据单位为百分比（如 0.1% 表示 10 bps），存储时转换为小数（如 0.001）。若无真实价差数据，使用日度波动率 σ_daily 的 10% 作为代理，并明确标注数据来源。

### 2.3 辅助特征

- **ADV**：过去 20 个交易日的日均成交量（股数）
- **Q/ADV**：订单数量占 ADV 的比例
- **股票日内 Beta**：使用过去 5 个交易日的 10 秒频率收益率（基于 Open），对同期基准指数 10 秒收益率进行回归，取斜率系数作为日内 Beta。回归模型为包含截距的线性模型 `r_stock,t = α + β × r_index,t + ε_t`，使用 Ledoit-Wolf shrinkage 估计以降低估计误差（实现可使用 `sklearn.covariance.LedoitWolf`）。数据清洗规则：剔除股票停牌期间（成交量为 0）、涨跌停期间（价格不变）的所有 10 秒观测；若有效观测数 < 1000（约 2.8 小时），该证券当日 β 标记为"不可靠"。数据准备阶段需同步采集基准指数的 10 秒 OHLC 数据。附加监控：计算日度 Beta（过去 2 年日频数据）与日内 Beta 的差异，若 `|β_intraday - β_daily| / β_daily > 50%`，触发"Beta 不稳定"标记并在报告中标注；记录每个估计日期的有效观测数和 R²，低于 0.05 的 β 标记为"噪音"。

---

## 3. 核心成本分解：调整后的恒等式

所有成本以**母单到达价 `P_arr`** 为基准，单位 bps，买入方向（s=+1）成本正表示不利。

#### 分析层级声明

一级指标（IS、A、B1、B2）、二级指标（流动性成本、择时分布成本、市场环境成本）和三级指标（三层体系）均在**路由级**计算。母单级指标为路由级指标按成交数量的加权均值。所有跨路由比较使用路由级指标，母单级仅用于内部审计和合规报告。

### 3.1 基础定义

- `P_arr` = 母单到达时刻所在 10 秒区间的 Open（mid_proxy）。假设所有订单成交均在开盘到收盘期间（含开盘与收盘），不产生跨日跳空
- `VWAP_int` = 路由实际工作区间 [`time_first_fill`, `time_last_fill`] 内市场的**真实 VWAP**（以成交额加权）。**切片边界规则**：工作区间定义为包含首笔成交切片起点至包含末笔成交切片终点的闭合区间。若首笔成交落在切片中间（如 09:35:07），该切片全额纳入；末笔同理。区间 VWAP 计算包含该区间内所有完整 10 秒切片，不按时间比例拆分首尾切片。在数据质量附录中记录首尾切片占比，若首尾切片合计成交额超过总区间 10%，标注"切片粒度不足"警告
- `VWAP_int,mid` = 同一区间以 mid_proxy 加权的均价：  
  `VWAP_int,mid = Σ (mid_proxy_i × 市场成交量_i) / Σ 市场成交量_i`  
  其中 `市场成交量_i` 为区间第 i 个 10 秒切片的**全市场成交量（股数）**，与 §2.2 中"成交量_i"为同一概念，与计算 VWAP_int 的成交量口径一致
- `P_avg` = 路由成交均价 = `Σ exec_price × exec_qty / Σ exec_qty`
- `φ` = 成交率 = `qty_filled / qty_route`。未完全成交处理见 3.6 节

### 3.2 第一层：总执行差额 IS 的会计恒等式（含等待成本拆分）

定义首笔成交到达价：

- **路由级**：`P_first_route` = 该路由首笔成交所在 10 秒区间的 Open（mid_proxy）
- **母单级**：`P_first_parent = Σ(P_first_route_i × qty_filled_route_i) / Σ qty_filled_route_i`，即母单下各路由首笔成交价按成交数量加权平均

```
IS_route = s × (P_avg - P_arr) / P_arr × 10^4
```

恒等分解：

```
IS = (A) 算法执行贡献
   + (B1) 等待漂移
   + (B2) 工作区间调度漂移
```

其中：

- **(A) 算法执行贡献**：`s × (P_avg - VWAP_int) / P_arr × 10^4`
- **(B1) 等待漂移**：`s × (P_first_route - P_arr) / P_arr × 10^4`
- **(B2) 工作区间调度漂移**：`s × (VWAP_int - P_first_route) / P_arr × 10^4`

**解释与归责：**
- (A) 反映算法在工作区间内相对市场均价的表现。内部进一步拆分为可控与不可控两部分（见 3.3 节），跨路由比较以可控部分"算法净贡献_调整后"为准。
- (B1) 捕获母单到达后至首笔成交的市场价格变动，归因于系统延迟、风控审查、路由排队等。使用 Open 基准后，B1 不再包含区间内前视偏差。母单级 B1 按各路由 B1 的成交量加权聚合。
- (B2) 捕获工作区间内整体市场漂移对成交的影响，主要体现调度时机选择。

汇总报告中可合并 `(B1)+(B2)` 为总调度漂移 (B)，但必须保留子项供泄露和延迟诊断。

### 3.3 第二层：(A) 算法执行贡献的诊断分解（含三层指标体系）

定义**成交量加权中间价**：
```
VWAP_exec,mid = Σ(exec_qty_j × mid_proxy_j) / Σ exec_qty_j
```

则 (A) 恒等分解为：

```
(A) = 流动性成本 + 择时分布成本 + 市场价差与冲击成本
```

- **流动性成本（算法可控）**：`s × (P_avg - VWAP_exec,mid) / P_arr × 10^4`  
  成交价相对同时点 Open 的溢价，主要体现算法获取流动性的能力（包括限价单选择、激进程度等）。

- **择时分布成本（算法可控）**：`s × (VWAP_exec,mid - VWAP_int,mid) / P_arr × 10^4`  
  算法成交量时间分布与市场成交量分布的差异，反映 VWAP 跟踪质量。

- **市场价差与冲击成本（环境，非可控）**：`s × (VWAP_int,mid - VWAP_int) / P_arr × 10^4`  
  反映市场整体买卖价差及日内冲击，仅用于标定环境，不纳入算法绩效评价。

#### 三层指标体系（解决价差成本归属冲突）

为消除价差成本在"算法净贡献"与"环境成本"之间的口径不一致，建立三层指标：

**第一层 | IS（总执行差额）**
- IS = (A) + (B1) + (B2)
- 用途：合规报告、与行业基准对标、总成本归因

**第二层 | 算法净贡献\_调整后**（扣除半价差的结构性成本）
```
算法净贡献_调整后 = 流动性成本_调整后 + 择时分布成本
流动性成本_调整后 = 流动性成本 - half_spread × (qty_filled / qty_route) × 10^4
```
其中 `half_spread = 0.5 × spread_proxy`。`spread_proxy` 和 `half_spread` 均以小数存储（如 spread_proxy = 0.001 表示 10 bps，half_spread = 0.0005 表示 5 bps），公式中乘以 10^4 将小数转换为 bps。`(qty_filled / qty_route)` 为路由级成交量占比，各路由独立承担自身成交部分的半价差成本。
- 用途：**跨路由/跨算法执行质量的核心比较指标**。已剥离结构性半价差成本，仅反映算法本身执行能力的差异

**第三层 | 半价差成本与市场冲击成本**（独立展示）
- 半价差成本 = `half_spread × (qty_filled / qty_route) × 10^4`
- 市场冲击成本 = `s × (VWAP_int,mid - VWAP_int) / P_arr × 10^4`
- 用途：环境标定、成本结构归因，不参与算法绩效排名

**恒等验证**：
```
(A) = 算法净贡献_调整后 + 半价差成本（纳入环境项）+ 市场冲击成本
```

母单级半价差成本 = Σ（各路由半价差成本 × 路由成交量）/ Σ 路由成交量。

鉴于使用 10 秒 Open 作为代理中间价，所有项均标注"代理（proxy）"，并建议用区间 Close 替代 Open 进行稳健性检验（见 3.8 节）。

### 3.4 第三层：调度漂移 (B) 的市场剥离

利用基准指数 10 秒数据，定义：
- `I_arr` = 母单到达时刻所在区间指数的 Open（mid_proxy）
- `I_first` = 首笔成交时刻所在区间指数的 Open（mid_proxy）
- `I_last` = 末笔成交时刻所在区间指数的 Open（mid_proxy）

**注意**：指数不适用"VWAP"概念（无成交额加权），直接使用指数本身的 Open 值，指数变动代表市场整体走势。

剥离公式：
- **B1 市场部分**：`B1_mkt = s × β_intraday × (I_first - I_arr) / I_arr × 10^4`
- **B1 个股残差**：`ε_B1 = B1 - B1_mkt`
- **B2 市场部分**：`B2_mkt = s × β_intraday × (I_last - I_first) / I_arr × 10^4`
- **B2 个股残差**：`ε_B2 = B2 - B2_mkt`

其中 `β_intraday` 为 §2.3 中的日内 Beta（基于过去 5 个交易日 10s 收益率回归）。

总调度漂移：
```
B = B_mkt + ε_idio
B_mkt = B1_mkt + B2_mkt
ε_idio = ε_B1 + ε_B2
```

### 3.5 冲击持久性补充（统一基准模块）

为保持与 IS 体系一致，采用统一的 Open 基准：

- **执行冲击**：`s × (P_avg - P_arr) / P_arr × 10^4` （等同于 IS）
- **反转**：`s × (P_post - P_avg) / P_arr × 10^4`
- **持久部分**：`s × (P_post - P_arr) / P_arr × 10^4`

其中 `P_post` 为末笔成交后 T+5 或 T+30 分钟的 Open。Beta 调整可类比 3.4 节对持久部分实施。对于收盘参与型订单（工作至收盘），`P_post` 直接采用收盘区间的 Open，分析归入"收盘算法执行质量"独立模块，不与常规盘中执行混排。解释为"价格运动净效应"，不严格区分临时/永久冲击。

### 3.6 未完全成交处理规则

- **部分成交且后续取消**：延展 IS 假设未成交部分按末笔成交的执行价格（单笔价）成交，此延展 IS 仅用于内部审计，不纳入路由评分卡的中位数计算
- **主动取消或限价未触及**：归入"取消/未执行"单独分组，其延展 IS 和成交率 φ 单独汇报，不参与相对排名
- φ 作为辅助指标，结合限价松紧度分层分析未成交原因；定期用少量实际完整成交回测延展 IS 偏差，评估假设合理性

### 3.7 恒等式的数学验证

可以证明恒等式在数学上精确成立（不含近似）：

```
IS = s(P_avg - P_arr)/P_arr × 10^4
   = s(P_avg - VWAP_int + VWAP_int - P_arr)/P_arr × 10^4
   = s(P_avg - VWAP_int)/P_arr × 10^4 + s(VWAP_int - P_arr)/P_arr × 10^4
   = (A) + s(VWAP_int - P_first_route + P_first_route - P_arr)/P_arr × 10^4
   = (A) + s(P_first_route - P_arr)/P_arr × 10^4 + s(VWAP_int - P_first_route)/P_arr × 10^4
   = (A) + (B1) + (B2)  ✓
```

同样可验证 (A) 的子分解恒等式。

#### 子项经济解读局限

所有分解子项（A、B1、B2、流动性成本、择时分布成本、市场环境成本）均以 `P_arr` 为统一分母以保证恒等式严格成立。因此：

- **B2（工作区间调度漂移）** 的量级受 `P_arr` 而非 `P_first_route` 影响，不能独立解读为"工作区间内的市场变动百分比"；
- 当 `P_first_route` 与 `P_arr` 差异较大（如延迟期间价格跳变 > 20 bps）时，B2 的经济含义被系统性缩放；
- 跨订单 B2 的比较应在 `P_first_route ≈ P_arr`（延迟较小）的子集中进行。

报告模板中对 `|P_first_route - P_arr| > 20 bps` 的订单标注"延迟较大，子项解读需谨慎"。

### 3.8 稳健性检验

鉴于使用 Open 作为代理基准价，建议针对关键结论实施稳健性检验：
- **替代基准检验**：使用每个 10 秒区间的 Close 替代 Open，重新计算全套 IS 分解
- **排名一致性检验**：计算两种基准下算法净贡献\_调整后 排名的 Spearman 秩相关系数
- 若 ρ < 0.9，说明基准选择对路由相对排名有实质影响，需在报告中标注"基准敏感"并使用两者的保守下限

### 3.9 层级聚合代数（路由级 → 母单级）

给定母单 P，包含路由集 {R₁, R₂, ..., Rₙ}。

**路由级指标**：X_R ∈ {IS, A, B1, B2, 流动性成本, 择时分布成本, 市场环境成本, 算法净贡献_调整后}，每个路由独立按 §3.1–§3.3 计算。

**母单级指标**：成交量加权聚合

```
X_P = Σ(w_R × X_R) / Σ w_R  ，其中 w_R = qty_filled_R（路由成交量）
```

**特殊处理**：
- `P_arr`：所有路由共用母单到达时刻的 Open（母单级唯一值）
- `P_first_parent`：按各路由 P_first_route 的成交量加权聚合（见 §3.2）
- 母单成交率 `φ_P = Σ qty_filled_R / qty_parent`
- 半价差成本_parent：按各路由半价差成本的成交量加权聚合（见 §3.3）

**评分卡口径约定**：
- 评分卡中"母单数"指独立的 parent_id 计数
- IS 中位数、算法净贡献_调整后中位数等分布统计基于路由级指标计算（每个路由为独立观测）
- 母单级指标仅用于内部审计和合规报告，不参与路由排名

---

## 4. 路由比较与绩效评价

### 4.1 核心比较指标

由于当前基建不具备预期成本模型的持续部署与校准能力，不计算 E[IS] 及 Score。路由比较直接基于实际执行成本：

| 指标 | 用途 | 说明 |
|------|------|------|
| **算法净贡献\_调整后** | 主排名（路由分配决策） | 已扣除结构性半价差成本，反映算法执行能力 |
| **IS** | 辅助排名（总成本视角） | 包含市场环境成本，用于合规报告和总成本归因 |
| **成交率 φ** | 辅助诊断 | 结合限价松紧度分析 |

### 4.2 双层排名体系

为区分"算法本身好不好"与"总成本高不高"，建立双层排名：

**主排名（用于路由分配决策）**
- 按**算法净贡献\_调整后**中位数排序
- 负值表示执行质量优于市场均价（考虑结构成本调整后），值越低（越负）越好
- 决定"给哪个路由分配更多流量"

**诊断排名（用于算法优化反馈）**
- 按**IS**中位数排序
- 同时展示其分解项（流动性成本、择时分布成本、市场环境成本）
- 反映订单整体的执行经济性，反馈给交易团队和算法提供商

**排名差异分析**：
当同一条路由在两层排名中位置差异显著时（如算法净贡献\_调整后排第 1 但 IS 排第 3），自动分析差异来源：
- 是否因市场环境成本异常偏高（如该路由分配了更多高波动时段订单）
- 是否因调度漂移 (B) 系统性偏高（如路由延迟较大）
- 差异说明自动标注在评分卡的"备注"列

### 4.3 分层比较（缓解选择偏差）

为避免算法分配导致的不可比性，按以下维度分层后在层内比较：

- **Q/ADV 分档**：按订单规模分为 3 档（如 <1%、1%-5%、>5%），各档内独立比较算法净贡献\_调整后
- **波动率分档**：按到达时刻 σ_daily 分为高/中/低波动率环境
- **方向**：买入/卖出独立计算

各分层的最低样本量要求：每个子组 ≥ 15 笔订单才纳入比较。

### 4.4 缺失协变量处理

若订单缺少必要字段（如限价类型、ADV），该订单仍参与 IS 及分解计算，但在分层比较中标记为"未分层"，单独分组展示。缺失率超过 5% 时在报告中标注数据质量问题。

---

## 5. 统计推断与实验设计

### 5.1 描述与可视化

- 汇报算法净贡献\_调整后及 IS 的中位数、IQR、90% 分位，以及均值供参考（严禁仅汇报均值）
- 分布图（箱线图、小提琴图）按路由对比
- 时间序列图：算法净贡献\_调整后及 IS 的移动中位数，监测绩效漂移

### 5.2 显著性检验

采用规范的 Bootstrap 方法，具体参数：

**重抽样方案**：
- 按交易日聚类重抽样（cluster by date），保证日内相关性结构不被破坏
- 每次 Bootstrap 抽取的日期数 = 原始数据日期数
- 重复 B = 2000 次（以确保 BCa 方法精度）

**置信区间**：
- 默认使用 BCa（Bias-Corrected and Accelerated）方法，95% 置信水平
- 若 BCa 无法收敛（通常因样本过小），回退至 Percentile 方法并标记
- 同时报告 Bootstrap 标准误 `SE_boot = sd(θ_b)`，其中 `θ_b` 为各次 Bootstrap 的统计量

**小样本处理（n < 30 笔订单）**：
- 不执行 Bootstrap 显著性检验
- 仅报告描述性统计，标注"样本不足，未做推断"

**检验类型**：
- 两两路由的算法净贡献\_调整后差的 BCa 置信区间，若 95% CI 不包含 0，标记为显著
- 单路由 IS 是否显著异于零：同样方法构造置信区间

**报告格式示例**：
```
路由 A 算法净贡献_调整后中位数: -1.8 bps [95% BCa CI: -3.2, -0.5]
(基于 45 个交易日, 320 笔订单, 2000 次 Bootstrap)
```

### 5.3 选择偏差缓解

当前不具备随机化实验条件。在分层比较（§4.3）基础上，可选用以下辅助方法作为稳健性分析：
- **倾向得分匹配（可选，远期）**：估计算法分配概率，匹配相近订单后比较，结果与分层比较交叉验证

随机化实验框架作为远期规划：
- **方式一：路由级随机化**。符合条件母单随机分配至算法 A 或 B
- **方式二：母单内配对拆分**。同一母单拆分为两部分随机分配（要求规模 > 1% ADV）
- 前提：IT 系统支持实时随机化与拆分，并排除极端大单；当前标记为"条件不具备"

### 5.4 泄露诊断（搁置）

> **状态：搁置，待后续版本重新评估。**

泄露诊断模块当前搁置，原因如下：
- ε_idio 不拆分，无 ε_suspicious 候选池；
- 执行前成交量异常检测需要逐笔买卖方向数据，当前数据不可得；
- 三条件触发规则的统计功效和假阳性率需在数据积累更充分后重新校准。

若后续恢复该模块，诊断条件需重新设计，不绑定当前的 ε_idio 拆分框架。

### 5.5 异常行为监控

在无预期成本模型的情况下，异常检测基于相对离群值：

- **成本异常**：单笔订单 `IS > 路由 IS 的 95% 分位`，记录并标注"成本异常-待复核"
- **模式异常**：单日某路由的算法净贡献\_调整后超出该路由月度 3 倍 IQR 范围，标记为"执行异常日"
- 异常记录按月汇总，供交易员和算法团队复核，不作为实时阻断依据

---

## 6. 报告与看板体系

### 6.1 执行驾驶舱（日内/实时）

- 当前活跃路由的算法执行贡献 (A) 实时值（标注"至今实现"）
- 基于同算法类型历史订单的完成时间分布，估计当前订单的预期完成时刻
- 实时成交量曲线 vs 市场成交量曲线（使用当前累计成交量占比与时间的关系）
- 单笔异常警报：实时 IS 或 (A) 超出同算法滚动均值 ± 3 标准差时预警

### 6.2 月度路由评分卡

| 路由 | 母单数 | ★算法净贡献_调整后(bps) | 排名 | IS中位数(bps) | 排名 | 流动性成本 | 择时分布成本 | 市场环境成本 | 等待漂移(B1) | 工作漂移(B2) | ε_idio均值 | 成交率 | 显著标记 | 备注 |
|------|--------|--------------------------|------|---------------|------|------------|--------------|--------------|--------------|--------------|------------|--------|----------|------|
| ...  |        |                          |      |               |      |            |              |              |              |              |            |        |          |      |

按**算法净贡献\_调整后**中位数排序（负值好），主排名优先级高于 IS 排名。附加 Bootstrap 显著性标记（与对照路由比较的 95% BCa CI）。"备注"列自动标注两层排名差异 ≥ 2 位的可能解释。

### 6.3 单笔诊断下钻

- IS 瀑布图：分解至算法净贡献\_调整后、半价差成本、市场环境成本、B1、B2
- 成交量分布图：市场成交量直方图 vs 算法成交量直方图，叠加 Open 走势
- 成交明细表：时间、价格、Open（mid_proxy）、价差代理、累积成交量
- 冲击持久性独立面板：统一以到达 Open 为基准，显示执行冲击、反转、持久部分

### 6.4 情景与趋势分析

- 按市场情景（上涨/下跌/震荡，基于区间 Open 变化）分组展示
- 泄露风险标记面板（当前搁置，待后续版本恢复）
- 异常行为汇总面板（月度成本异常与执行异常日统计）

---

## 7. 落地路线图

| 阶段 | 目标 | 核心工作 | 输出 |
|------|------|----------|------|
| **Phase 0** | 数据基础设施 | 建立 10s OHLC 数据管道（证券 + 基准指数）；生成 Open 基准并校验质量（缺失率、日内覆盖率）；构建母单/子单/成交流水表；计算 ADV、日内波动率、价差代理；积累≥1 个月历史数据用于后续统计 | 数据质量报告、历史数据就绪 |
| **Phase 1** | 基础 IS 与恒等式 | 实现母单/路由 IS、(A)、(B1+B2) 恒等分解；路由 IS 与 (A) 基础报表；统计分布描述（中位数、IQR） | 路由 IS 与 (A) 月报 |
| **Phase 2** | 诊断模块上线 | 实现三层指标体系（算法净贡献\_调整后分解）；流动性/择时/市场环境成本分离；日内 Beta 回归管道上线；B 的市场剥离；单笔下钻视图 | 月度诊断评分卡（主排名+辅助排名） |
| **Phase 3** | 统计推断与分层 | 实现 BCa Bootstrap 置信区间（2000 次、按日期聚类）；Q/ADV 与波动率分层比较；两两路由显著性检验；小样本保护规则上线 | 带显著性标记的评分卡、分层分析报告 |
| **Phase 4** | 全算法扩展与自动化 | 引入非 VWAP 算法，对齐诊断维度（例如 IS 算法的择时成本以方案预设轨迹衡量）；自动化报告与异常预警；对接经纪商评审流程；随机化实验框架就绪但标记"待启用" | 全算法绩效管理驾驶舱、随机化实验设计文档 |

---

## 8. 方案局限性声明

1. **基准近似**：基于 10 秒 Open 作为代理中间价，忽视买卖价差和亚 10 秒价格运动。Open 为区间起点价格，无法反映区间内的中间价变化，流动性成本测量存在低估。与基于 tick 级中间价的其他 TCA 系统不可直接比较绝对水平，但相对排名在稳健性检验通过（ρ ≥ 0.9）时具有可比性
2. **等待期测量**：`P_first` 为 10 秒区间 Open，无法区分瞬时延迟与区间内波动，B1 包含系统延迟与市场自然等待，归因精度受限
3. **冲击拆解简化**：反转分析不区分临时与永久冲击；所有价格统一为 Open，但微观结构噪音（如收盘价买卖失衡）对 T+30 分钟反转仍可能产生影响
4. **无难度调整**：当前不实施预期成本模型，跨订单比较依赖分层（Q/ADV、波动率）缓解选择偏差，但无法完全消除订单特征差异对排名的影响。分层变量在样本不足时无法执行
5. **价差数据依赖**：真实价差缺失时采用波动率代理，微观结构成本测量误差增大
6. **随机化实验可行性**：当前无法实时随机化与拆分，因果推断受限；分层比较仅为缓解措施
7. **未完全成交处理**：延展 IS 基于假设未成交部分按末笔成交执行价格成交，可能高估或低估真实成本，结果独立呈现且不与正常订单混排
8. **日内 Beta 稳定性**：基于过去 5 个交易日 10s 频率回归的日内 Beta，在流动性不足的证券上可能估计误差较大，需配合"Beta 不稳定"标记使用
9. **统一半价差扣除假设**：三层指标中的半价差成本统一扣除假设所有成交均为主动吃单（支付价差），对以挂单为主的限价算法可能过度调整（挂单赚取的价差被不当扣除）。需获取逐笔成交方向数据后方可实现方向感知调整

尽管存在上述局限，本方案通过统一的 Open 基准（消除前视偏差）、无遗漏的 IS 恒等式、三层指标体系（剥离结构性价差成本）和规范的 Bootstrap 统计推断，在现有数据条件下可有效实现跨路由相对绩效评价和 VWAP 执行诊断，是当前最严谨可行的执行分析框架。

---

## 附录 A：方案修订记录

| 版本 | 日期 | 修订内容 |
|------|------|----------|
| v1.0 | 初始版 | 完整方案（含紧急度、聚类、预期成本模型、刷基准检测） |
| v2.0 | 当前版 | ① mid_proxy 由 (H+L)/2 改为 Open；② 移除 Parkinson 波动率；③ 移除紧急度分组；④ 移除算法风格聚类；⑤ 指数 VWAP 改为指数值；⑥ 日度 Beta 替换为日内 Beta；⑦ 移除预期成本模型及 Score；⑧ 重构三层指标体系（解决价差成本归属）；⑨ 双层排名体系；⑩ Bootstrap 规范化（BCa、2000 次）；⑪ 泄露诊断三条件（已搁置）；⑫ 移除刷基准检测 |

## 附录 B：Open 基准下偏差传播分析

**复核结论**：将 mid_proxy 由 (High+Low)/2 改为 Open 后，前视偏差和范围中点偏差均已消除，IS 恒等式中不再存在因基准定义导致的系统性扭曲。具体分析：

1. **前视偏差消除**：Open 是区间起点已知价格，P_arr（到达时刻区间 Open）不包含到达之后的任何价格信息，不再有 Look-Ahead Bias
2. **范围中点偏差消除**：不再使用 (High+Low)/2，避免了上涨行情中基准高估、下跌行情中基准低估的系统性问题
3. **剩余偏差**：Open 可能不等于区间内的真实中间价（区间内若存在开盘跳空，Open 与区间成交均价有偏离），但此偏差在所有子项中使用相同 Open 基准，在恒等式中会大幅抵消。剩余残差属于测量误差范畴，非结构性缺陷，忽略剩余残差。
