"""Regime distribution cross-module contracts.

Currently empty — regime distribution is queried via CostViewDatabaseAdapter
which returns plain dicts. Regime-specific contract types will be added here
when a cross-module consumer needs typed access (YAGNI principle).
"""
