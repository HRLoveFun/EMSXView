# EMSX 项目下一步功能构建路线图

>本文档聚焦项目本身的迭代规划——什么该做、什么不该做、按什么顺序做。核心原则:**项目不是要做下一个 Bloomberg,而是要做一个 buy-side fill data 上的执行研究平台**。

---

## 一、项目定位再校准

### 1.1 战略定位

> **用真实的 buy-side fill data, 做别人做不了的 execution research。"**

这是项目最重要的定位。所有功能取舍都围绕它判断:**这个功能是否强化"研究平台"属性?如果只是 EMS 工程能力的展示,优先级降一档。**

### 1.2 三层架构再认识

| 层 | 现状 | 战略地位 | 投入策略 |
|---|------|---------|---------|
| ExecutionView (订单执行) | 🟢 production-ready 核心 | 工程基础设施,**不是差异化** | 保持稳定,够用即可,不重写 |
| MarketView (Pre-trade) | 🟡 仅 shell + snapshot | **决策引擎**,差异化关键 | 大规模投入(P2 优先级) |
| CostView (Post-trade) | 🟢 数据管道在,UI 集成 | **研究平台**,最大差异化 | **最大投入**(P1 优先级) |

**关键认知:** 大多数 EMS 把 CostView 做成 reporting tool(IS、VWAP slippage 数字图表),这不是研究平台。你要把它升级成**execution alpha research environment**——可以提出假设、跑实验、得出带置信区间的结论。

---

## 二、P1:CostView 升级为 Execution Alpha Research Platform

**目标:** 把现有的描述性 TCA 升级成可以做 hypothesis testing 的研究环境。

### 2.1 模块 A:Market Regime Classification

将每笔历史 fill 打上 regime 标签,作为后续所有研究的分组维度。

**功能列表:**

1. **Volatility regime**
   - Realized vol(20D / 60D)分位数
   - Implied vol分位数(从期权数据获取，需要特别处理)
   - VIX / 各市场对应 vol index 的状态
   - 输出:每笔 fill 在执行时点对应的 vol regime label(low/mid/high/extreme)

2. **Liquidity regime**
   - 标的当日 turnover vs 30D 平均
   - Quoted spread 分位数
   - Order book depth(如果数据可得)
   - 输出:liquidity regime label

3. **Trend / mean-reversion regime**
   - 短期 momentum(过去 5/20 日)
   - 与长期均线偏离度
   - 输出:trending up / trending down / range-bound 标签

4. **Time-of-day bucket**
   - Open auction / morning / midday / afternoon / closing auction
   - 不同市场需要分别建模(US vs Asia vs EU)

5. **Macro event flag**
   - FOMC、CPI、地缘事件日 ±1d 的标记(可手工维护一个 calendar)
   - 这一项手工成本小,但研究价值极大

**技术栈建议:**
- 数据层:在 `CostView/src/` 下加 `regime_classifier.py`,接入历史价格、IV 数据
- 存储:每笔 fill 加 regime feature 列,存为 Parquet
- 可视化:在 frontend 的 CostView 标签下加 regime distribution 视图

**重点任务:** 数据接入和验证

### 2.2 模块 B:Algo Performance Attribution

这是真正的 research 层。

**功能列表:**

1. **多维度切片分析**
   - By broker × algo × regime 的 slippage 表
   - By participation rate bucket(< 5%、5–10%、10–20%、> 20%)
   - By order size bucket(以 %ADV 为单位)
   - 默认指标:IS slippage、VWAP slippage、reversal(成交后短期价格回归方向)

2. **Conditional algo selection 推荐**
   - 给定订单 (size, ADV, IV, regime),输出历史上表现最好的 algo
   - 用 mean ± 95% CI 展示,避免过度拟合
   - **关键**:必须有 sample size 提示,n < 30 的 cell 不出推荐

3. **统计显著性检验框架**
   - Bootstrap resampling 计算 slippage 分布的 CI
   - Welch's t-test 比较两个 algo 的均值差
   - **多重检验校正**:Benjamini-Hochberg FDR(因为你会同时跑很多组对比)
   - 这一块是和"普通 TCA dashboard"的根本分野——后者只给数字,你给的是统计推断

4. **Reversal analysis(信息泄漏检测)**
   - 成交后 1min / 5min / 30min 的价格回归
   - 高 reversal = 算法可能在 leak signal,impact 没有被恰当摊销
   - 这是 buy-side 评估 broker algo 质量的核心指标之一

**技术栈建议:**
- `CostView/src/research/` 下新建 attribution 模块
- 用 `scipy.stats` + `statsmodels` 做检验
- 输出层:Jupyter notebook templates(用 papermill 参数化)+ frontend 可视化


### 2.3 模块 C:Reproducible Research Output

让你的研究产出可以拿出去看。

**功能列表:**

1. **Parameterized notebook templates**
   - 一个标准模板:输入(date range, instrument universe, regime filter),输出(slippage report + attribution + significance tests)
   - 用 papermill 或 Quarto 实现
   - 每次研究跑完,产出一份带数据、代码、结论的可分享文档

2. **Research note generator**
   - 标准 sell-side research note 格式(2–3 页 PDF)
   - 自动生成结论 + 关键图表 + 方法论附录

3. **数据版本化**
   - 每个研究输出绑定一个数据快照 hash
   - DVC(Data Version Control)或简易自建版本化
   - 让结果可追溯、可复现


---

## 三、P2:MarketView 升级为 Pre-Trade Decision Engine

**目标:** 把 pre-trade 从"看行情"升级成"基于历史 + 当前状态的算法决策推荐"。

**价值:** 这一层把 CostView 的 backward-looking research 转化为 forward-looking decision support,闭环成为完整的研究→决策→执行→反馈系统。

### 3.1 模块 D:Order Sizing & Impact Estimation

**功能列表:**

1. **%ADV 计算与可视化**
   - 对每个准备下的订单,实时计算 size / 30D ADV
   - 给出"这单在历史上属于多大订单"的分位
   
2. **Empirical impact model**
   - 在自己的 fill data 上拟合 square-root impact:`IS = α × σ × sqrt(size/ADV)`
   - 按 instrument liquidity bucket 分别拟合
   - 输出 expected pre-trade cost(bps)

3. **Pre-trade cost decomposition**
   - Spread cost + impact cost + timing risk
   - 经典 Almgren-Chriss 框架的可视化
   - 让交易员看到"这单理论 expected cost 多少 bps"

**技术栈建议:**
- 在 `MarketView/` 下建立 `impact_model.py`
- 用 scipy.optimize 做参数拟合
- 模型参数定期(月度)重训,版本化存储

### 3.2 模块 E:Algo Recommendation Engine

**功能列表:**

1. **基于 CostView 历史数据的推荐**
   - 输入:instrument、size、当前 regime
   - 查询:historical fills 在最相似 regime 下的最佳 algo + participation
   - 输出:top 3 推荐 + 历史表现 CI

2. **决策解释层(关键)**
   - 不要给黑箱推荐
   - 显示:"推荐 GS IS @ 8% participation,因为在 high-vol + low-liquidity regime 下,该组合在过去 N 笔类似订单中 mean slippage = X bps,95% CI [a, b]"
   - **可解释性是 buy-side trader 接受推荐的前提**

3. **A/B 实验框架(进阶)**
   - 对类似订单随机分配两种 algo
   - 累计样本到统计显著为止
   - 这是真正工业级 execution research 的做法,做出来在简历上是非常硬的素材


### 3.3 模块 F:Closed-Loop Feedback

让 MarketView 的推荐结果回流到 CostView 评估。

**功能列表:**

1. **每笔订单标记"是否采纳推荐"**
2. **采纳 vs 未采纳的 slippage 对比**
3. **推荐模型的 out-of-sample 评估**

这一层一旦跑通,你就有了完整的 ML/research lifecycle:**train(CostView 历史)→ deploy(MarketView 推荐)→ monitor(采纳率与表现)→ retrain**。

**预计工时:** 2 周

---

## 四、P3:Optimal Execution Layer

**目标:** 把项目从"经验研究"上升到"理论 + 经验"。

**对你职业的价值:** 国内 execution 领域几乎没有人能 cover stochastic optimal control。

### 4.1 模块 G:Almgren-Chriss Baseline

**功能列表:**

1. **闭式解实现**
   - Risk-neutral case:linear trajectory
   - Risk-averse case:exponential decay trajectory
   - 输入:total size、time horizon、impact parameters、risk aversion λ
   - 输出:optimal trading trajectory + expected cost + risk

2. **参数标定**
   - Permanent impact γ:从同向后续 fill 的 price drift 推断
   - Temporary impact η:从短期 reversal 推断
   - σ:从 realized vol 直接得到

3. **与 broker algo 实际 trajectory 对比**
   - 把实际 fill 的 cumulative volume curve 画出来
   - 叠加 Almgren-Chriss 理论 trajectory
   - 量化偏离度


### 4.2 模块 H:HJB-based Extensions(进阶)


**功能选项(选 1–2 个深做):**

1. **Stochastic volatility extension**
   - 把 σ 从常数变成 Heston 或 SABR 过程
   - 求解对应的 HJB 方程(数值解,有限差分)
   - 比较和常数 vol 假设下 trajectory 的差异

2. **Adverse selection extension(Cartea-Jaimungal 框架)**
   - 引入 short-term alpha signal
   - HJB 求解最优 trading rate
   - 这是连接 alpha 和 execution 的桥梁,在 multi-strategy fund 里特别值钱

3. **Multi-asset optimal execution**
   - portfolio-level optimal execution 问题
   - 涉及协方差矩阵和 cross-asset impact
   - 这是 quant fund rebalancing 的核心问题

**技术栈建议:**
- 数值 HJB 求解:numpy + scipy,或者更进阶用 JAX(自动微分对 HJB 很友好)
- 可视化:trajectory + value function 的 3D 可视化(Plotly)


### 4.3 这一层的产出

不只是代码,还要有:

- **一份 technical writeup**(10–15 页 PDF):从问题到 HJB 推导到数值实现到与实际数据对比。
- **一篇配套 blog post**:把上面的 writeup 简化成给非数学背景读者也能看懂的版本。

---

## 五、不该做的事(避免精力误投)

| ❌ 不要做 | 原因 |
|----------|------|
| 重写 OMS / 完整订单管理系统 | Bloomberg、Charles River、Aladdin 已经做完了,正面竞争无意义 |
| 低延迟 / 高频架构 | 与 buy-side execution research 主线无关,且需要 C++/FPGA,精力陷阱 |
| ML 黑箱 algo selection 模型 | 解释性差,buy-side trader 不会用;且容易过拟合到小样本 |
| 多用户多租户、权限系统 | 工程复杂度高,对 research 方向无加分 |
| 实时行情可视化美化 | Bloomberg 已经做完了,你做不出竞争力 |
| 接入更多 broker / 更多市场 | 只是覆盖度,不是深度;深耕已有的数据更有价值 |


**判断原则:** 如果一个功能不能让你讲出"这里我用了 X 方法,得到了 Y 洞察"——那就不该做。

---

## 六、12 个月迭代时间表

| 月份 | 主线 | 输出物 |
|------|------|--------|
| **M1** | Regime classifier + fill data 标注 | 全部历史 fills 完成 regime 标签 |
| **M2** | Algo attribution + 显著性检验 | 第一份 internal research note(broker algo 对比) |
| **M3** | Research notebook templates + 第一篇 blog post | Blog post 1 发布 |
| **M4–M5** | MarketView impact model + pre-trade cost engine | Pre-trade decision engine v1 |
| **M6** | Algo recommendation engine + 决策解释层 | 推荐系统上线;Blog post 2 |
| **M7** | Closed-loop feedback + 推荐效果监控 | 完整的 research-decision-feedback 闭环 |
| **M8–M9** | Almgren-Chriss baseline + 参数标定 | Optimal execution module v1;Blog post 3 |
| **M10–M11** | HJB extension(选一个深做) | Technical writeup PDF |
| **M12** | 整理项目作为 portfolio;GitHub README 重写 | portfolio 完整 |

---

## 七、GitHub 作为 Portfolio 的呈现策略

项目终将放上 GitHub(或部分公开,fill data 脱敏)

### 7.1 README 的关键内容(顺序)

1. **一句话 positioning**:"A buy-side execution research platform built on top of Bloomberg EMSX, with focus on optimal execution theory + empirical broker algo evaluation."
2. **关键差异化**:不是另一个 OMS,是研究平台,数据是真实 buy-side fill data(脱敏)。
3. **架构图**:用现在的三层架构图,但强调 research output 是核心产物。
4. **样例研究输出**:展示 1–2 个 research note 的截图(脱敏后)。这是最有说服力的部分。
5. **理论基础**:列出 Almgren-Chriss、Cartea 等的实现部分,链接到 references。
6. **技术栈**:简洁列出。
7. **About the author**:链接 LinkedIn 和 blog。

### 7.2 提交习惯

- Commit message 写得专业(用 conventional commits)
- 每个重要功能开 PR(即使是自己合自己),展示工程纪律
- 关键模块写 design doc(在 `docs/` 下)
- **关键 commit 数量** > **总 commit 数量**:招聘官扫 GitHub 时看的是峰值能力,不是数量

### 7.3 配套内容

- **Blog**:Substack / 个人站均可,中英双语版本
- **LinkedIn 长帖**:每个重要模块上线时发一篇,带项目链接
- **可选:一两个 Twitter/X 上的 quant 圈子**:`@quantminded`、`@QuantsCorner` 这类账号偶尔会转有质量的内容

---

## 八、一个核心检查问题

每个季度问自己一遍:

> **如果一个 head of execution research 现在打开我的项目,能不能在 10 分钟内看到一个让他愿意约我聊聊的研究产出?**

如果答案是"还看不到",说明 P1 的研究层投入不够,要加码;不要去新功能。

如果答案是"看到了",说明项目已经在产生杠杆,可以继续按节奏推进 P2 / P3。

---

## 九、与日常工作的协同节奏

- **每周 5–8 小时**项目时间(工作日 1–2 小时 + 周末 3–5 小时),持续 12 个月足以走完上面的 P1 + P2。
- **P3 的 HJB extension 集中在 1–2 个月内 bursting 完成**,别拖,因为它是连续的数学工作,中断成本高。
- **每完成一个模块,立即更新 GitHub README + 写 blog post**。研究产出的复利在表达,不在代码。

---

## 十、最终目标

12 个月后,你项目应该读起来像这样:

> **EMSX Execution Research Platform** *(personal project, 2025–2026)*
> 
> Built an end-to-end execution research platform on top of Bloomberg EMSX, integrating pre-trade decision support, order execution, and post-trade analysis with a focus on empirical broker algo evaluation and optimal execution theory.
> 
> - Designed a regime-conditional algo attribution framework with bootstrap-based statistical inference, identifying X% slippage reduction opportunities across N broker-algo combinations on Y fills of real buy-side flow.
> - Implemented Almgren-Chriss optimal execution with empirically calibrated impact parameters; extended to stochastic volatility regime via numerical HJB solution.
> - Architected a closed-loop research-to-decision feedback system: pre-trade impact estimates inform algo selection; post-trade attribution retrains the recommendation model.
> - Tech stack: Python (FastAPI, pandas/polars, scipy), React/TypeScript, Bloomberg API.


到这一步,项目就完成了它的使命。

---

## 十一、存储与备份方案（长期规划锚点）

> 本节是研究平台的数据基础设施长期规划。M1（Regime 分类层）当前直接读现有 SQLite，但所有新模块通过 `CostView/src/storage/` 占位接口访问数据，为后续实施分层与归档预留切换点。

### 11.1 数据规模现状（2026-04-27 实测）

| 项 | 值 |
|---|---|
| 数据日期范围 | 2025-09-25 → 2026-04-22（147 trading days）|
| 活跃 ticker 数 | ~1989 |
| `raw_bdib.db` | 68.5 GB（占 78%）|
| `processed_fills.db` | 14.6 GB |
| 其他 DB（fills/raw_bdib 派生/fill_bdib） | ~5.4 GB |
| **总计** | **~88 GB** |
| **日增量** | **~600 MB/trading day** |
| **年增量预估** | **~150 GB/year** |
| 本地磁盘 | 1 TB（剩余 ~932 GB） |
| 异地备份盘 K:\ | SMB 共享 / 千兆 / 4.7 TB 总 / 651 GB free |

Regime 层新增数据可忽略不计（< 1 MB/日）。

### 11.2 存储分层（P0a，M1 之后启动）

**目标**：raw_bdib SQLite ≤ 25 GB（hot 区），保证查询性能；冷区 100% 可访问。

| 层 | 范围 | 格式 | 路径 |
|---|---|---|---|
| HOT | 最近 12 个自然月 | SQLite | `CostView/data/*.db` |
| COLD | >12 个月历史 | Parquet（按 year/month 分区，跨表统一归档） | `CostView/data/archive/year=YYYY/month=MM/` |

**归档单元 = "一个交易月跨所有表的全部数据"**：
- 同一个 `year=YYYY/month=MM/` 目录下打包：raw_bdib_part-{0..7}.parquet、raw_fills.parquet、processed_fills.parquet、fill_bdib.parquet、regime_*.parquet、archive_manifest.json
- 研究复现时拿一个目录就能跑完该月所有 fill 的 attribution，不必混合 SQLite + Parquet

**模块设计（占位接口先行）**：
```
CostView/src/storage/
├── tiering_config.py        # HOT_RETENTION_DAYS=365, ARCHIVE_ROOT
├── parquet_archive.py       # 写 Parquet、按分区路径管理、原子提交
├── archive_manager.py       # 月度归档主流程
├── unified_reader.py        # 统一查询接口（HOT/COLD 自动路由）
└── archive_audit.py         # 归档审计表
```

**M1 期间约束**：
- 所有新代码（regime 层）通过 `unified_reader` 读取 raw_bdib，不直接 `sqlite3.connect`
- 当前 `unified_reader` 内部就是直连 SQLite 的薄壳，未来分层时只换实现

**月度归档自动化流程**（设计中）：
```
Step 1: 选出 trade_date 在 [now − 12mo − buffer, now − 12mo) 的所有数据
Step 2: 按 (year, month) × table 写 Parquet 到 *.tmp
Step 3: 校验行数 + SHA-256 hash
Step 4: 原子 rename + 写入 archive_audit
Step 5: 从 SQLite DELETE + VACUUM
```

**容量影响**：
| 阶段 | 本地占用 | 等效寿命 |
|---|---:|---:|
| 当前 | 88 GB | ~5.5 年 |
| 启用分层后（5 年累积） | 25 GB SQLite + 110 GB Parquet ≈ 135 GB | **15+ 年** |

### 11.3 K 盘异地备份（P0b，与分层并行设计）

**定位**：个人异地备份（非团队共享）。本地灾难、误删除、数据漂移的最后一道防线。

**目录结构**：
```
K:\CHEN Hongru\EMSX_Backup\
├── manifest\backup_manifest.json         # 每次同步元信息
├── databases\
│   ├── current\                          # 最新版（覆盖式）
│   └── snapshots\YYYY-MM-DD\             # 历史快照（gzip）
├── archive\year=YYYY\month=MM\           # Parquet 归档镜像（append-only）
├── reference\                            # market_mapping、macro_calendar
├── exports\                              # 脱敏后的研究产出
└── logs\
```

**快照策略（应对逻辑错误，非硬件故障）**：

| 类型 | 频率 | 保留 | 大小（gzip 后） |
|---|---|---|---:|
| current | 每日覆盖 | 永久最新 | ~45 GB |
| daily snapshot（小库：raw_fills/fill_bdib/regime） | 每日 | 14 日 | ~14 GB |
| weekly snapshot（processed_fills 大库） | 每周日 | 8 周 | ~30 GB |
| monthly snapshot（全库） | 每月末 | 12 个月 | ~145 GB |
| yearly snapshot（全库） | 每年末 | 永久 | ~12 GB/年 |
| raw_bdib HOT | 仅 current（可从 Bloomberg 重拉） | — | — |
| Parquet archive | 增量复制（不可变） | 永久 | 与本地一致 |

**首年总占用**：~235 GB（K 盘 651 GB free 充足）。

**安全控制**：
- 同步走 SHA-256 双向校验 + manifest 状态机（`staged → verified → committed`）
- snapshots/archive 目录 ACL 设为 append-only
- exports/ 目录强制走 `anonymize_for_export`（脱敏 trader、size→%ADV bin）
- 每周日自动 `verify` 子命令对比 K 盘 vs 本地 hash
- 季度跑一次 `restore_test.py` 演练灾难恢复

**调度（Windows Task Scheduler）**：
| 任务 | 时间 |
|---|---|
| sync-databases | 每日 19:00（在 daily_update 18:00 之后） |
| sync-archive | 每月 1 日 03:00 |
| verify | 每周日 04:00 |
| restore_test | 每季度首日 |

### 11.4 实施顺序（与 M1-M3 协同）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **当前 M1** | Regime 分类层 + storage 占位接口 | 进行中 |
| **P0a**（M1 之后） | raw_bdib 冷热分层 + 月度归档自动化 | 设计完成，未实施 |
| **P0b**（与 P0a 并行可行） | K 盘 sync 脚本 + 快照策略 + manifest 校验 | 设计完成，未实施 |
| **P0c**（P0a/b 完成后） | ACL 配置 + 灾难恢复演练 | — |
| **M2 起** | Attribution、Reproducible Output 等按原计划推进 | — |

### 11.5 风险与监控

| 风险 | 缓解 |
|---|---|
| raw_bdib ticker universe 扩大 2x，存储寿命减半 | 启用分层后，COLD 区 Parquet 压缩 3-5x，仍可承受 |
| K 盘网络中断 | sync 失败重试 3 次后告警；不影响本地操作 |
| 数据加工 bug 写脏一周数据 | daily snapshot 14 日窗口足够发现并回滚 |
| 误删本地 DB | K 盘 current + snapshots 双保险 |
| 归档后查询变慢 | unified_reader 加 LRU 缓存（key=ticker+date_range，TTL=1h） |

---


